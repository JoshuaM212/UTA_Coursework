// Mech-Lab 2 Example
// Dario Ugalde and Joshua Martinez

//-----------------------------------------------------------------------------
// Hardware Target
//-----------------------------------------------------------------------------

// Target Platform: EK-TM4C123GXL Evaluation Board
// Target uC:       TM4C123GH6PM
// System Clock:    40 MHz

// Hardware configuration:
// GPIO Output:
//   PD0 enable for coil A
// GPIO Output:
//   PD1 enable for coil B
// GPIO Output:
//   PA4 driver pin for coil A+
// GPIO Output:
//   PC4 driver pin for coil A-
// GPIO Output:
//   PC5 driver pin for coil B+
// GPIO Output:
//   PC6 driver pin for coil B-
// GPIO Input:
//   PD6 input for IR Collector

//-----------------------------------------------------------------------------
// Device includes, defines, and assembler directives
//-----------------------------------------------------------------------------

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include "clock.h"
#include "uart0.h"
#include "wait.h"
#include "motor_control.h"
#include "tm4c123gh6pm.h"

// Bitband aliases
#define PIN_1           (*((volatile uint32_t *)(0x42000000 + (0x400043FC-0x40000000)*32 + 2*4)))   //PA2
#define PIN_2           (*((volatile uint32_t *)(0x42000000 + (0x400043FC-0x40000000)*32 + 3*4)))   //PA3
#define PIN_3           (*((volatile uint32_t *)(0x42000000 + (0x400043FC-0x40000000)*32 + 4*4)))   //PA4
#define PIN_4           (*((volatile uint32_t *)(0x42000000 + (0x400063FC-0x40000000)*32 + 4*4)))   //PC4
#define PIN_5           (*((volatile uint32_t *)(0x42000000 + (0x400063FC-0x40000000)*32 + 5*4)))   //PC5
#define PIN_6           (*((volatile uint32_t *)(0x42000000 + (0x400063FC-0x40000000)*32 + 6*4)))   //PC6
#define PIN_LED         (*((volatile uint32_t *)(0x42000000 + (0x400073FC-0x40000000)*32 + 6*4)))   //PD6

// PortA masks
#define PIN_1_MASK 4    //PA2
#define PIN_2_MASK 8    //PA3
#define PIN_3_MASK 16   //PA4

// PortC masks
#define PIN_4_MASK 16   //PC4
#define PIN_5_MASK 32   //PC5
#define PIN_6_MASK 64   //PC6

// PortD masks
#define PIN_LED_MASK 64  //PD6


//-------------------------------------------------------------------------------------------

#define MAX_CHARS 80
#define MAX_FIELDS 5

double saved_angle_state = 0;
double desired_angle_state = 0;

typedef struct _USER_DATA
{
    char buffer[MAX_CHARS+1];
    uint8_t fieldCount;
    uint8_t fieldPosition[MAX_FIELDS];
    char fieldType[MAX_FIELDS];
} USER_DATA;

void getsUart0(USER_DATA* data)
{
    uint8_t count = 0, i = 0;
    while ( i < MAX_CHARS+1 )
    {
        data->buffer[i++] = '\0';
    }
    while(1)
    {
        char c = getcUart0();
        if ( ( c == 8 || c == 127 ) && count > 0 )
        {
            count--;
        }
        else if (c == 13)
        {
            data->buffer[count++] = '\0';
            return 0;
        }
        else if (c >= 32)
        {
            data->buffer[count] = c;
            count++;
            if ( count ==  MAX_CHARS )
            {
                data->buffer[count] = '\0';
                return 0;
            }
        }
    }
}

void parseFields(USER_DATA* data)
{
    uint8_t count, m = 0;
    data->fieldCount = 0;
    while ( m < MAX_FIELDS )
    {
        data->fieldPosition[m] = '\0';
        data->fieldType[m] = '\0';
        m++;
    }

    char previous_char = '\0';

    // a = Alpha  65-90 & 97-122
    // n = Numeric 48-57 & 46 & 45
    // d = Delimiter
    for ( count = 0; count <= MAX_CHARS; count++ )
    {
        if (data->fieldCount < MAX_FIELDS)
        {
            char current_char = data->buffer[count];

            if ( (current_char >= 65 && current_char <= 90 ) || ( current_char >= 97 && current_char <= 122 ))
            {
                if (previous_char == '\0')
                {
                    data->fieldType[data->fieldCount] = 'a';
                    data->fieldPosition[data->fieldCount] = count;
                    data->fieldCount++;
                }
            }
            else if ((current_char >= 48 && current_char <= 57 ) || ( current_char == 45 ) || ( current_char == 46 ))
            {
                if (previous_char == '\0')
                {
                    data->fieldType[data->fieldCount] = 'n';
                    data->fieldPosition[data->fieldCount] = count;
                    data->fieldCount++;
                }
            }
            else
            {
                current_char = '\0';
                data->buffer[count] = '\0';
            }
            previous_char = current_char;
        }
        else
        {
            char current_char = data->buffer[count];
            current_char = '\0';
            data->buffer[count] = '\0';
        }
    }
    return 0;
}

char* getFieldString(USER_DATA* data, uint8_t fieldNumber)
{
    uint8_t i = data->fieldPosition[fieldNumber];
    uint8_t j = 0, m = 0;
    char get_string[20];

    while ( m < 20 )
    {
        get_string[m++] = '\0';
    }
    while (data->buffer[i] != '\0')
    {
        get_string[j++] = data->buffer[i++];
    }

    if ( ( fieldNumber <= data->fieldCount )  && ( data->fieldType[fieldNumber-1] == 'a' ) )
    {
        return get_string;
    }
    else
    {
        return NULL;
    }
}

int32_t getFieldInteger(USER_DATA* data, uint8_t fieldNumber)
{
    uint8_t i = data->fieldPosition[fieldNumber];
    uint8_t j = 0;
    uint8_t m = 0;
    char num_string[20];

    while ( m < 20 )
    {
        num_string[m++] = '\0';
    }
    while (data->buffer[i] != '\0')
    {
        num_string[j++] = data->buffer[i++];
    }
    //double num_out = strtoul(num_string,NULL, 10);
    double num = atof(num_string);

    if ( ( fieldNumber <= data->fieldCount ) && ( data->fieldType[fieldNumber] == 'n' ) )
    {
        desired_angle_state = num;
    }
    else
    {
        desired_angle_state = 999;
    }
}


//-------------------------------------------------------------------------------------------

uint8_t direction = 0;          // direction 0 = cw, direction 1 = ccw
uint8_t cur_state = 0;          // Keeps track of the step state machine

double cur_deg_per_step = 1.8;  // Change # based on steps -> currently full-step, 1.8 degrees each **************************

uint8_t dir_state_mngr = 0;     // state 1 -> cw used last, state 2 -> ccw used last

void time_btwn_steps(int state) // WIP: state 1 = can be modified to test top speed available
{                               //      state 2 = used to realign the motor to 0 degree in the beginning
    if (state)
    {
        waitMicrosecond(50000);
    }
    else if (state == 2)
    {
        waitMicrosecond(100000);
    }
}

void direction_manager (int last_know_dir, int curr_dir)
{
    if (last_know_dir == curr_dir)  // if both the prev and curr direction are same, do nothing
    {
        dir_state_mngr = curr_dir;
    }
    else                            // if different, changed the current state accordingly
    {
        if (dir_state_mngr)     // cw was prev used
        {
            if ( (cur_state == 2 ) || (cur_state == 3 ) )
            {
                cur_state= cur_state - 2;
            }
            else if ( cur_state == 1  )
            {
                cur_state = 3;
            }
            else if ( cur_state == 0  )
            {
                cur_state = 2;
            }
        }
        else                // ccw was prev used
        {
            if ( (cur_state == 0 ) || (cur_state == 1 ) )
            {
                cur_state= cur_state + 2;
            }
            else if ( cur_state == 2  )
            {
                cur_state = 0;
            }
            else if ( cur_state == 3  )
            {
                cur_state = 1;
            }
        }
        dir_state_mngr = curr_dir;      // save the last used state
    }
}

void motor_state(int cur_state)     // Motor stepping pin outputs
{



    if (cur_state == 0)
        {
            PIN_3 = 1;
            PIN_4 = 0;
            PIN_5 = 0;
            PIN_6 = 0;
        }
        if (cur_state == 1)
        {
            PIN_3 = 0;
            PIN_4 = 0;
            PIN_5 = 1;
            PIN_6 = 0;
        }
        if (cur_state == 2)
        {
            PIN_3 = 0;
            PIN_4 = 1;
            PIN_5 = 0;
            PIN_6 = 0;
        }
        if (cur_state == 3)
        {
            PIN_3 = 0;  // PA4  -> Coil A Positive
            PIN_4 = 0;  // PC4  -> Coil A Negative
            PIN_5 = 0;  // PC5  -> Coil B Positive
            PIN_6 = 1;  // PC6  -> Coil B Negative
        }
}

void motor_step(int direction, int speed_state)
{
    direction_manager (dir_state_mngr, direction)
    motor_state(cur_state);
    if (direction)  // sets motor going cw , state 0->3
    {
        if (cur_state == 0)
        {
            cur_state = 3;
        }
        else
        {
            cur_state--;
        }
    }
    else        // sets motor going ccw, state 3->0
    {
        if (cur_state == 3)
        {
            cur_state = 0;
        }
        else
        {
            cur_state++;
        }
    }
    time_btwn_steps(speed_state);
}

motor_dir_by_angle(double old_angle, double new_angle, uint8_t direction, int speed_state) // 45 degree limit = 90 total
{
    double temp_old = old_angle;
    double temp_new = new_angle;

    //--------MISALIGNED DIRECTIONS-TEST-FIXES----------------------------------------------------
    if (direction)
    {
        while (temp_old < temp_new)
        {
            motor_step(direction,speed_state);
            temp_old = temp_old+cur_deg_per_step;
        }
    }
    else if (!direction)
    {
        while (temp_old > temp_new)
        {
            motor_step(direction,speed_state);
            temp_old = temp_old-cur_deg_per_step;
        }
    }
    //----------------------------------------------------------------------------------
    saved_angle_state = temp_old;
}


/////////////-----REMOVE----LATER---/////////////////////////////////////////
void setPwm(uint16_t COIL_A, uint16_t COIL_B)
{
    PWM1_0_CMPA_R = COIL_A;
    PWM1_0_CMPB_R = COIL_B;
}
////////////////////////////////////////////////////////////////////////////



//-----------------------------------------------------------------------------
// Subroutines
//-----------------------------------------------------------------------------

// Initialize Hardware
void initHw()
{
    // Initialize system clock to 40 MHz
    initSystemClockTo40Mhz();

    // Enable clocks
    SYSCTL_RCGCGPIO_R |= SYSCTL_RCGCGPIO_R0 | SYSCTL_RCGCGPIO_R2 | SYSCTL_RCGCGPIO_R3;  // enables ports A, C, and D
    _delay_cycles(3);

    // Configure pins
    GPIO_PORTA_DIR_R |= PIN_1_MASK | PIN_2_MASK | PIN_3_MASK;  // marks port value as digital output
    GPIO_PORTA_DEN_R |= PIN_1_MASK | PIN_2_MASK | PIN_3_MASK;

    GPIO_PORTC_DIR_R |= PIN_4_MASK | PIN_5_MASK | PIN_6_MASK;  // marks port value as digital output
    GPIO_PORTC_DEN_R |= PIN_4_MASK | PIN_5_MASK | PIN_6_MASK;

    GPIO_PORTD_DIR_R &= ~PIN_LED_MASK;      // mark port as input
    GPIO_PORTD_DEN_R |= PIN_LED_MASK;       // enable port
  //  GPIO_PORTD_PDR_R |= PIN_LED_MASK;       // pull down resistor

}

//-----------------------------------------------------------------------------
// Main
//-----------------------------------------------------------------------------

int main(void)
{
    initHw();
    initPwm();
    initUart0();
    setUart0BaudRate(19200, 40e6);

    USER_DATA data;

    //Enable Pins
    PIN_1 = 1;      //PA2
    PIN_2 = 1;      //PA3
    int i = 0;

    while (!PIN_LED)  // PD6 handles while switch
    {
        motor_step(direction, 2);       // 2 -> slow speed when traversing steps, 1 -> faster
    }

    direction = 1;

    for (i = 0; i < 23; i++)  // takes about 22 to reset to 0 degrees
    {
        motor_step(direction, 2);
    }

    putsUart0("\n\n-------Lab 2 Test-----\n");  // Echo back to the user of the TTY interface for testing

    while(true)
    {
        bool valid = false;
        putsUart0("enter cmd: ");
        getsUart0(&data);                           // Get the string from the user
        parseFields(&data);                          // Parse fields
        getFieldInteger(&data, 0);

        if ( ( desired_angle_state >= -90 ) && ( desired_angle_state <= 90 ) )
        {
                valid = true;
        }
        if (valid)
        {
            if (saved_angle_state >= desired_angle_state)
            {
                direction = 0;
            }
            else
            {
                direction = 1;
            }
            motor_dir_by_angle(saved_angle_state, desired_angle_state, direction, 1);
        }

        if (!valid)
        {
            putsUart0("\nInvalid command\n\n");
        }
    }
}

/*

 void positive_step()
{
    PIN_3 = 0;
    PIN_4 = 0;
    PIN_5 = 0;
    PIN_6 = 1;
    time_btwn_steps();
    PIN_3 = 0;
    PIN_4 = 1;
    PIN_5 = 0;
    PIN_6 = 0;
    time_btwn_steps();
    PIN_3 = 0;  // PA4  -> Coil A Positive
    PIN_4 = 0;  // PC4  -> Coil A Negative
    PIN_5 = 1;  // PC5  -> Coil B Positive
    PIN_6 = 0;  // PC6  -> Coil B Negative
    time_btwn_steps();
    PIN_3 = 1;  // PA4  -> Coil A Positive
    PIN_4 = 0;  // PC4  -> Coil A Negative
    PIN_5 = 0;  // PC5  -> Coil B Positive
    PIN_6 = 0;  // PC6  -> Coil B Negative
    time_btwn_steps();
}

void negative_step()
{
    PIN_3 = 1;  // PA4  -> Coil A Positive
    PIN_4 = 0;  // PC4  -> Coil A Negative
    PIN_5 = 0;  // PC5  -> Coil B Positive
    PIN_6 = 0;  // PC6  -> Coil B Negative
    time_btwn_steps();
    PIN_3 = 0;  // PA4  -> Coil A Positive
    PIN_4 = 0;  // PC4  -> Coil A Negative
    PIN_5 = 1;  // PC5  -> Coil B Positive
    PIN_6 = 0;  // PC6  -> Coil B Negative
    time_btwn_steps();
    PIN_3 = 0;
    PIN_4 = 1;
    PIN_5 = 0;
    PIN_6 = 0;
    time_btwn_steps();
    PIN_3 = 0;
    PIN_4 = 0;
    PIN_5 = 0;
    PIN_6 = 1;
    time_btwn_steps();
}



    // step 9 loop: 16*1.8 degrees CCW to 16*1.8 degrees CW w/ 1s delays
    while (true)
    {
        for (i = 0; i < 4; i++)
        {
            positive_step();  // goes ccw
        }
        i = 0;
        for (i = 0; i < 4; i++)
        {
            negative_step();    // goes cw
        }
    }




    //--------MISALIGNED DIRECTIONS SAVED-BACKUP----------------------------------------------------
    if (!direction)
    {
        while (temp_old < temp_new)
        {
            motor_step(1,speed_state);
            temp_old = temp_old+cur_deg_per_step;
        }
    }
    else if (direction)
    {
        while (temp_old > temp_new)
        {
            motor_step(0,speed_state);
            temp_old = temp_old-cur_deg_per_step;
        }
    }
    //----------------------------------------------------------------------------------



    */